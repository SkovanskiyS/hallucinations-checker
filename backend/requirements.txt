# Core API
fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.40
pydantic>=2.8,<3.0

# AI Model (optional — backend works in heuristic mode without these)
# Install with: pip install -r requirements.txt
# To skip ML dependencies, comment out the lines below.
lettucedetect>=0.1.7
torch>=2.1
transformers>=4.42
